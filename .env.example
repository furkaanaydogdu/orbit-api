# Used by the Postgres container itself (docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=orbit

# Used by the NestJS app directly (must match the values above)
DATABASE_URL="postgresql://postgres:your_password_here@localhost:5432/orbit"

PORT=3000